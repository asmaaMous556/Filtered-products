import { subCategory } from './products';

export interface category{
    id:number,
     name:string,
     nameAr:string,
     nameEn:string,
     photo:string,
     subCategories:subCategory[],
     
 
 }
 
 